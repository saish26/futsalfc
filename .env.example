# Base URL axios uses. "/backend" goes through the Next rewrite (avoids CORS).
# Point it straight at the API (e.g. https://api.example.com) once CORS is enabled there.
NEXT_PUBLIC_API_BASE_URL=/backend

# Where the /backend rewrite forwards to (read at build/start time).
API_PROXY_TARGET=http://localhost:8080
